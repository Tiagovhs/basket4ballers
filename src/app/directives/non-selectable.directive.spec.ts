import { NonSelectableDirective } from './non-selectable.directive';

describe('NonSelectableDirective', () => {
  it('should create an instance', () => {
    const directive = new NonSelectableDirective();
    expect(directive).toBeTruthy();
  });
});
