import { ColorNumberCardDirective } from './color-number-card.directive';

describe('ColorNumberCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNumberCardDirective();
    expect(directive).toBeTruthy();
  });
});
